Key Value (Google cash)
==

V get(K key);
put(K key, V value)

Есть двусвязный список, get ищет в hashmap ноду и перекидывает в начало.
put фигачит в начало. Если элементов больше чем N, выкидывает последний.

Придумать инвариант и проверять его с помощью assert.